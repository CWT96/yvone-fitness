# 课时账户交付记录

## 数据库

`202609240001_session_accounts.sql` 已于 2026-09-24 通过 Supabase 官方 CLI 管理连接，在 `zhyxlzalrpwfsqpxfftt` 执行成功。用户明确授权代执行。仅增量迁移，不重跑旧初始化，不改其他项目。

- `session_entries` 是不可直接修改的流水：购课、调整、按次扣课、包月上课。
- `monthly_memberships` 保留有效期、金额、说明及作废原因。
- 所有写入通过教练专用函数；学生仅能读取本人的数据，停用学生无访问权。
- 购课、调整和包月录入使用请求 UUID 防止网络重试重复入账；课程扣除与完成状态在同一事务，预约唯一约束防重复扣除。
- 包月按课程开始日和工作室时区判断。按次余额可以为负，方便教练补录和核对。
- 旧完成课程不回溯扣除，补录/作废包月不重算已有扣课。纠错保留调整原因，不删除原始流水。

执行后只读核验：新表均开启 RLS、完成扣课触发器启用、流水 0、包月记录 0、历史已完成数 0 与迁移前相同。未创建生产测试数据。

## 验证

68 项测试通过，包含买 3 节完成 1 节余 2 节、重复请求不重复增减、预约/改期/取消不扣、包月起止边界与美西日期、作废保留历史、负数余额、调整校验、跨学员隔离、直接写表及非教练函数调用被拒绝。迁移在本地重复执行验证幂等。

浏览器本机演示验证：教练总览和单人账户、完成后余 2 节及 1 小时历史、续购加课、负数调整、包月登记后未来课程不再算按次需求、学员仅见本人且无管理按钮。390px 学员页面无整页横向溢出，浏览器无警告或错误日志。

最终发布通过 Git main 自动触发 Vercel yvone-fitness 独立项目。旧资料无需迁移；已有线下余课仍需教练提供并录入准确的期初数量，代理不会猜测。

## 后续维护

本机 Supabase CLI 已存在授权管理登录，`db query --linked --project-ref ... --file ...` 可执行经测试和授权的增量脚本。每次务必显式指定正确项目。禁止读取/打印 token 或使用 `db push/reset` 重跑无迁移历史的旧数据库。
