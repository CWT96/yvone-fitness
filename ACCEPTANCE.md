# 上线验收清单

本地已有 29 项通过的规则测试；以下必须在真实 Supabase / Vercel / Resend 上完成，不要仅凭演示数据验收。

## 账号与隔离

- 使用绑定邮箱的一次性教练邀请码注册，完成邮箱验证，获得教练界面。
- 无邀请码或过期邀请码注册失败；用错邮箱不能使用绑定邀请。
- 创建两个学员账号 A、B，验证 A 看不到 B 的个人资料、预约留言、训练计划与档案。
- 直接调用 Supabase 注册接口、不填写邀请码仍应失败。
- 学员修改自己的 role/referral_code 或代别人预约应被数据库拒绝。
- 关闭/恢复学员账号，分别验证读写被限制/恢复。
- 密码重置、修改邮箱确认链路可用。

## 排课

- 教练开放两个未来时段，重叠时段无法提交。
- A 预约第一个时段，带留言；教练能看到。
- A 和 B 在两台设备抢同一空闲时段，只有一个成功，另一个收到冲突提示。
- 教练代 B 预约；B 能看到预约。
- A 改期到第二个时段，原时段重新开放，改期原因和操作记录保留。
- A 取消后时段重新开放，B 的预约不受影响；取消原因保留。
- 课程开始后学员不能自行改期/取消；教练可处理。
- 课程结束后仅教练可标记完成。
- 美西冬夏令时均正确；春季跳时中不存在的 02:30 不可创建。

## 计划、档案、推荐

- 给 A 创建草稿，A 不可见；发布后仅 A 可见，B 不可见。
- 发布第二份计划，第一份成为历史计划，只有一份当前计划。
- 私密档案只有教练可见；共享后只对指定学员可见。
- 用 A 的固定推荐码注册 C：验证前「待验证」，验证后成功数加一，不重复计数。
- 后台查看总体、A、B、C 各自推荐汇总和明细；导出文件中文正常。
- 停用邀请码后不能再注册。

## 邮件与支付窗口

- Resend 域名已验证，Supabase SMTP 能向普通学员邮箱发送验证/恢复邮件。
- 预约、改期、取消、发布计划后，队列增加对应通知，Cron 自动发送。
- 检查 Cron 运行记录及网站邮件状态，不能只验证手动发送。
- 关闭学员邮件开关后，后续通知跳过；教练开关独立。
- 改期或取消后旧提醒跳过，不发送错误课前时间。
- 邮件接口无凭据/学员凭据请求应拒绝；教练凭据或正确 Cron secret 可调用。
- 支付窗口展示正确方案和价格，但收款按钮保持禁用。

## 手机与恢复

- 手机宽度导航、表单、预约列表和横向时间表可操作；弹窗错误信息可见。
- 新开浏览器、刷新网页仍能读取真实数据；演示提示已消失。
- 退出后无法从页面访问前一个账号的数据。
- Vercel/数据库失败时显示错误，而非误提示保存成功。
