# 网站检查与修复 · 2026-09-23

## 检查范围

本轮检查教练/学员界面、数据库权限与业务规则、通知/联系人接口、桌面和 390px 手机布局。浏览器写操作使用本机演示数据，数据库测试使用隔离的 PGlite 数据库，邮件接口测试拦截全部外部请求；没有改动生产学员、真实预约或发送测试邮件。

## 已修复

1. 筛选学员后新建档案/计划，原来默认第一位学员，容易保存错人；现在沿用有效筛选，没有指定则必须主动选择。
2. 预约列表原来把最远的课程放最上面；现在进行中及未来预约优先、最近在前，历史记录倒序。
3. 数据保存成功但刷新失败，原来显示操作失败且保留提交表单；现在关闭已保存的表单，提示重新加载，避免重复提交。
4. 初次读取失败没有重试入口、后台刷新失败静默；现在有清晰的状态和重新加载按钮。
5. 账号切换/退出时旧请求可能回写，刷新登录令牌可能中断编辑；现在清空旧账号状态、使旧读取失效，令牌刷新不再重置编辑界面。
6. 计划标题、预约留言、档案、目标、电话和地点的输入长度与数据库限制不一致；现在在表单提前限制。价格按两位小数，邀请码次数和天数只接受整数，必填文字不接受全空格。
7. 修改邮箱后的查收提示被普通保存提示覆盖；现在保留明确的验证提示。注册/恢复提示注明在同一浏览器打开链接。
8. 注册、重置和修改密码增加确认密码；切换登录表单清理旧错误，提交期间禁用切换按钮。
9. 预约详情把无法读取姓名的教练显示成“学员”，关闭详情还提示保存成功；现在正确显示操作人角色，详情只读且保留换行。
10. 联系人同步状态被显示成“发送中/发送失败”；现在使用同步状态，并说明每分钟自动检查。
11. 手机收起的侧栏仍能被辅助技术/键盘访问；现在隐藏收起内容，菜单支持 Escape 关闭。弹窗有可访问名称，错误在弹窗内显示并滚动至可见位置。
12. 日历按访问者电脑时区加日期，跨夏令时可能与工作室日历不一致；现在按工作室日期连续显示七天，“本周”改为准确的“回到今天”。
13. CSV 导出忽略推荐人/状态筛选；现在与当前筛选一致。计划/档案筛选无结果时提示“当前筛选下没有”，避免误以为全部数据丢失。

## 实际验证

- 本地浏览器：教练代预约 → 改期含原因 → 取消含原因；结果和时段正确。
- 档案：按陈以宁筛选新建，确认归属陈以宁；保存草稿 → 发布 → 删除 → 恢复为草稿。
- 计划：未指定学员时不默认第一人；新建林予安草稿，学员不可见；发布后旧计划归档。
- 学员端：他人档案/草稿不可见；只有本人的单次/包月价格；教练修改后显示新金额；邮件开关关闭成功。
- 邀请码：限定邮箱创建 → 停用；推荐汇总/明细可查看。
- 手机：时间表、学员管理、计划、档案、推荐、价格、设置均无整页横向溢出；表格和时间表在自身区域横向滚动。注册及找回密码页面宽度正常。
- 浏览器未记录 error/warn。56 项自动测试通过，包括新增错人默认值、过期请求丢弃、保存后刷新失败、排序、跨时区夏令时日历用例。类型检查及正式构建通过。

## 验证边界

没有生产教练/学员登录凭据，本轮没有冒充完成真实邮箱验证、找回密码、改邮箱、真实收件或生产双账号并发端到端测试。上述功能的既有真实用户验收记录保留在 STATUS.md；本轮以本地界面、隔离数据库和接口测试覆盖。Stripe 仍未接入。此次不修改数据库结构，无需运行 SQL。
