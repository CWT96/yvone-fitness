# 交接状态 · 2026-09-22

## 2026-09-26 — 完整课程目录，线上线下分开

- 根据用户 PDF 采用课程分类与包含内容，不导入标价。用户确认不提供 1 对 2，线上指导不包含线下课程。
- 可购 8 个方案：线下 1 对 1 单次；5／10／20 节课次套餐；线下不限次包月含饮食指导；线上 1／3／12 个月。教练独立设置学员价格，新增价格默认留空。旧 3／12 个月不限次订单保留原义，购买入口退出新目录。
- `202609260004_course_catalog.sql` 已应用 Fitness 数据库。新增独立 online_memberships、专属价格与课次到期日；线上付款不增线下课时、不豁免线下扣课。支持教练手动录入、作废线上服务。
- 5／10／20 节付款入账与三个月有效期；课时统计按实际上课日期优先用最早到期的有效课时，单独展示过期未用数量，晚标记课程也按上课日判断。原有无期限课时不补设到期日。
- 购课分类支持后退和 URL 恢复。网站、Stripe 商品说明与新通知均覆盖英文；所有周期仍为一次购买、不自动续费。
- 验证：105 项测试、类型检查与正式构建通过。涵盖旧订单兼容、各新套餐准确入账、防重复、权限隔离、线上不覆盖线下、到期和晚标记。浏览器演示验证教练保存价格、学员分类与包含内容、后退导航；无浏览器错误。没有进行真实 Stripe 扣款或发送测试邮件。
- 使用说明见 `COURSE_CATALOG.md`；用户仍需为新增方案逐学员设置价格后才可购买。

## 2026-09-26 — 防邮箱扫描的认证验证码

- 注册、重置密码、邮箱变更增加中英文 `/auth/verify` 页面，只在用户提交时验证，访问页面不会消耗验证码。邀请注册与普通密码登录保留。
- 邮箱变更分别使用接收邮件的地址验证；第一封确认没有会话时继续提示完成另一封确认。
- 三份固定英文模板使用数字 Token，链接仅打开静态输入页。不关闭邮箱确认，不修改 SMTP、密钥、数据库或 Stripe。
- 必须按 `SUPABASE_EMAIL_LANGUAGE.md` 手动替换后台三份邮件正文；这取代下文“已有英文模板无需修改”的说明。尚未替换线上模板，也未发送真实测试邮件。
- 验证：100 项自动测试、类型检查与正式构建通过；包含验证码请求、过期错误、双邮箱第一步及模板无凭证链接检查。

## 2026-09-26 — 认证邮件保持英文

- 用户确认 Supabase 注册验证、密码重置及邮箱变更邮件保持固定英文，不按学员语言切换。
- 已将仓库中的三份备用模板和配置说明改为固定英文；现有后台模板若已英文，无需修改。没有操作 Supabase 后台模板。
- 此决定替代下文认证邮件双语模板的待办；网站及业务通知语言切换保持原实现。


## 2026-09-26 — 学员端中英文

- 登录/注册、学员全部栏目、预约确认与改期取消、错误提示、分页、课时和付款记录、完整购课须知增加 English / 中文切换。首次根据浏览器语言选择，已登录学员同步账号偏好，教练界面保持中文。浏览器标题统一为 Yvonne Fitness。
- 英文使用英文星期及 AM/PM，排课时区保持工作室设置；保存的时间、价格、课时及访问权限不变。教练手写计划、档案和备注保留原文。
- 系统预约/改期/取消/未到场/训练计划/购课邮件按收件人的语言生成英文标题、正文和须知；教练通知仍为中文。新 Stripe Checkout 按账号语言显示，金额、模式、一次性付款与防重复结算保持原逻辑。
- `202609260003_member_language.sql` 已应用至 Fitness 项目，仅增加可由本人修改的语言字段和注册语言元数据。原有账号在登录时保存浏览器/本地选择。
- Supabase 认证邮件的三份中英文条件模板已准备在 `supabase/templates/`；用户需按 `SUPABASE_EMAIL_LANGUAGE.md` 在后台粘贴保存。网站发布不会自动修改 Supabase 模板，不能将这一步描述为已完成。
- 验证：98 项自动测试通过（包括语言权限、模板覆盖、时区、英文邮件、Stripe 本地模拟），类型与正式构建通过；本地浏览器检查学员全部栏目、双语注册/改期、语言记忆与手机布局，无浏览器错误。没有发送测试邮件、创建真实订单或改动收款密钥。
- 用户此前已自行切换正式 Stripe，本次没有更改收款环境。下方较早的“保持 test”记录不再代表当前配置。


## 2026-09-26 — 用户验收后的七项体验改进

- 推荐表保存注册姓名快照；旧记录用可用注册信息补齐，不开放跨学员 profile 查询。
- 付款按钮仅被点击的方案显示正在打开，另一方案仅禁用以防重复提交。
- 栏目、学员筛选、搜索和周选择同步 URL；支持刷新、前进、后退。首次恢复登录不重置页面。
- 历史计划明确为发布同一学员的新计划后自动归档，不按时间过期。课程预约增加课后标记入口和规则说明。
- 教练预约对话框支持新增时间并直接预约；数据库同事务处理，失败不保留空时段。
- 学员近况和学员姓名进入综合看板：统计、状态、包月、专属价格、当前计划、最新档案、待标记课程及接下来预约。
- Migration 202609260001_member_workflows.sql 已应用到 Fitness 项目。88 项测试、类型检查、生产构建通过；浏览器验证刷新、后退和演示排课成功。
- 用户报告 Stripe 沙盒验收基本通过；保持 test 模式，正式收款需最后明确确认。

## 2026-09-25 — Stripe 测试支付

- 实现学员专属价格的一次性按次购买和包月购买；包月不自动续费。付款通知签名校验、重复事件防重、退款核对和每页 10 条订单记录已实现。
- 测试模式仅教练可操作，不增加真实课时、不发送购买邮件；学员只可查看自己的真实订单。
- 数据库更新已执行。87 项测试、类型检查和生产构建通过。专用测试密钥已保存，用户确认已配置付款通知和其余环境变量。
- 尚需发布和真实 Stripe 沙盒结账联调；未开启真实收款。详见 STRIPE_SETUP.md。

## 2026-09-25 — 未到场（No show）

- 新增课程结束后的教练专用「No show · 未到场」操作、确认扣课说明和可选缺席备注；待确认完成改为待确认结果。
- 用户已确认规则：按次缺席扣 1 节，包月内只记缺席；均不计入完成次数和训练时长。课时流水、预约筛选、总览及个人历史单独展示缺席，沿用每页 10 条和学员数据隔离。
- 新增 `202609250002_no_show.sql`，已通过 Supabase CLI 对 `zhyxlzalrpwfsqpxfftt` 执行。保留原始预约和流水；重复提交不重复扣课；缺席邮件包含时间、备注及结算结果，遵守通知开关。
- 验证：74 项测试通过、类型检查及生产构建通过。本地浏览器实测 3 节 → 缺席 1 次 → 余 2 节，完成次数和小时保持 0；学员端未显示另一人的缺席和扣课，浏览器无错误。
- 若误扣，可通过有原因的课时调整返还，原缺席事件仍保留。未增加自动扣课或付款功能。

## 2026-09-25 分页、美式单位与扩展档案

- 14 处长列表增加每页 10 条分页，筛选/账号变化重置、页数减少自动回退。统计与导出仍基于全量数据；取消邮件列表原来 100 条的展示截断。
- 体重输入/显示改为 lb，原 kg 存储保持兼容；新接口 `save_record_us` 转换输入，并在未改体重时保留旧值，避免多次编辑漂移。
- 档案增加 8 个选填指标：身高、腰/臀/胸/上臂/大腿围（in）、静息心率（bpm）、睡眠时长。可展开填写，未填不显示；沿用档案草稿/发布/删除与学员隔离权限。
- `202609250001_record_measurements.sql` 已使用官方 Supabase CLI 对 zhyxlzalrpwfsqpxfftt 执行，新增 measurements 字段及保存接口。旧记录数量与体重校验值在更新前后核对，未给生产学员写入测试数据。
- 71 项测试、类型检查、构建通过。浏览器本地验证 11 条档案/流水的 10+1 分页、筛选重置、末页删除回退、余额仍为全量 13 节、指标保存/编辑、旧重换算、390px 手机指标换行及翻页按钮可达，无浏览器错误。
- 扣课流程未修改：预约即成立，无教练预审批；结束后教练标记完成才扣 1 节，包月内不扣按次余额。用户第 4 项为咨询，未添加审批或签到流程。

## 2026-09-24 课时账户与统计

- 新增 `课时与统计`：购课入账、按次余额、包月有效期、手动调整及原因、每笔流水、历史完成次数/时长、本月次数、已预约未上；教练有汇总及余额不足筛选，学员只能看本人。
- 规则：教练确认完成后自动扣 1 节；预约、改期、取消不扣。包月按上课开始日期和工作室时区判断，含起止日，不扣按次余额。余额不足允许负数并提示核对，不丢失已完成课程。
- 用户在远程操作，明确授权代理代执行 SQL。发现本机 Supabase CLI v2.115.0 已有管理登录；成功使用 `supabase db query --linked --project-ref zhyxlzalrpwfsqpxfftt --file supabase/migrations/202609240001_session_accounts.sql` 执行，未读取或输出凭据。今后可优先检查此官方方式，无须默认让用户手工执行。
- 线上只读核验：两张新表存在且 RLS 开启；完成扣课触发器 1 个已启用；执行前后历史已完成数均为 0，初始流水和包月记录均为空。未写入生产购课或测试账号。
- 68 项本地测试、类型检查与正式构建通过。浏览器演示已实测买 3 节完成 1 节余 2 节、累计 1 小时；续购、调整、包月、本人资料隔离及 390px 布局。
- 旧已完成课程不会补扣；原有余课需教练核对后录入期初调整。Stripe 仍未连接，金额记录不发起收款。执行情况详见 `SESSION_ACCOUNTS_20260924.md`。

## 2026-09-24 实用体验改进

- 教练首页增加待确认课程、待制定计划、待设置价格的直接入口，仅将有效在训学员列入配置待办。今日课程优先显示进行中和未来安排。
- 学员列表支持直接代预约并预选该学员；未有当前计划时可继续已有草稿；课程卡片可写记录并带入上课日期和学员。
- 学员首页优先展示下一次课程和当前计划；时间表改为按日期分组的可预约列表，手机无需横向滑动；计划默认显示当前计划。
- 预约支持接下来、今天、待确认完成等筛选；进行中的课程不再提前消失。页面切换回到顶部，空结果和时间说明更明确。
- 59 项本地测试通过，覆盖新增状态/时区边界与既有权限隔离。浏览器演示验证代预约、学员预约、记录草稿、确认完成、当前计划筛选及 390px 手机布局。未操作生产学员资料或发送测试邮件。
- 无数据库迁移或环境变量变更。详细记录见 `UX_REVIEW_20260924.md`，本提交经 Git 自动部署；具体发布结果在会话中核验。

## 2026-09-23 全站检查

- 本轮修复与验证记录见 `QA_AUDIT_20260923.md`。核心修复包括筛选后新建归属、保存后刷新失败的重复提交风险、读取重试、过期账号请求回写、预约排序、表单限制、手机菜单与错误提示。
- 56 项本地规则/回归测试、类型检查和正式构建通过。没有改动生产测试学员或发送测试邮件；真实登录后的邮箱确认链路未由代理复测。
- 无数据库更新。配套界面由此提交部署，生产结果以 Vercel READY 及域名部署编号核实。

## 本次新增功能

- 训练计划/档案增加删除和恢复草稿；草稿/发布改成独立按钮。
- 两种学员专属价格：单次训练、不限次数包月。RLS 阻止读取他人价格，旧公共价格只对教练可见。Stripe 仍未连接。
- 已加入 Resend 联系人同步队列、自动重试及教练查看状态；只处理专属分组，不改已有全局退订状态或其他业务分组。
- 用户已确认执行 `202609220002_content_pricing_contacts.sql` 成功，已在 Vercel 保存专用 Full access `RESEND_CONTACTS_API_KEY`。已添加 `RESEND_SEGMENT_ID=1b66d482-f5bb-4c9f-bc3c-efbfe72acafc`，复用原 Cron。
- 本地 51 项测试、类型检查、正式构建通过。浏览器演示验证保存草稿、删除、恢复草稿、金额保存及学员仅显示本人两种价格。
- 本次功能提交 `c9e30a6456612b49460c34746f9438bbe64ea5b9` 已自动部署为 `dpl_8NFnss6rr5cKhR6NgSjTxF1r5cSJ`，READY、source=git；正式域名 HTML 返回 200 并匹配此部署。匿名联系人接口返回 401。授权通知任务返回 200，contacts.configured=true、本轮 synced=0/failed=0（无到期队列，不代表所有历史记录均已成功；分组人数等待用户后台核对）。

## 当前服务

- 正式网址：https://www.yvonnefitness.com 。https://yvonnefitness.com 已正确 308 跳转至 www；用户确认旧缓存问题已解决。备用：https://yvone-fitness.vercel.app 。
- Vercel：https://vercel.com/vinclo/yvone-fitness 。独立项目，未修改原 vinclo 项目的代码、域名或配置。
- 已验证的 Git 自动部署：dpl_2CZbn3qURHHNf2TE6yzyYXLavwNv（READY、production、source=git），对应 main 提交 663e7dc2a8a45e0d36da9b0e6f5b4b25c04b5b53。
- GitHub：https://github.com/CWT96/yvone-fitness 。用户明确要求暂时 Public，之后自行改 Private，仍为待办。
- 源码备份：../Yvone-Fitness-source.zip（Git archive，不含密钥及依赖）。
- 工作室名称 Yvone Fitness；教练初始邮箱 wt.cong96@gmail.com；业务时区 America/Los_Angeles。

## 已连接的平台

- Supabase 项目 zhyxlzalrpwfsqpxfftt，URL https://zhyxlzalrpwfsqpxfftt.supabase.co 。初始迁移及 202609220001_booking_email_details.sql 均已由用户执行。
- 注册开启邮箱验证，邀请码校验在数据库注册触发器执行；学员推荐码可直接用于注册。Supabase Auth Site URL 和主域名、www、备用域名的 callback/recovery 地址均由用户保存。
- Vercel Production 七项变量已齐全：NEXT_PUBLIC_SUPABASE_URL、NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY、SUPABASE_SERVICE_ROLE_KEY、RESEND_API_KEY、RESEND_FROM、SITE_URL、CRON_SECRET。用户私密 Key 直接填入平台，未读取其内容。
- Resend 使用 contact.yvonnefitness.com；用户于 2026-09-23 确认正确发件人名称为 Yvonne Fitness。已更新 Production `RESEND_FROM` 为 `Yvonne Fitness <appointments@contact.yvonnefitness.com>`，此文档提交触发重新部署以应用环境变量。使用独立 Key，未改 vinclo 原 Key。用户已自行修改 Supabase SMTP Sender name；预约通知的发件人由 Vercel 单独控制。既有已收邮件不会更新。
- Supabase Cron 每分钟调用 https://www.yvonnefitness.com/api/notifications。用户确认 SQL 执行成功，后续真实预约自动邮件成功。
- 本机私密 .env.cron-secret 和 .env.email-cron.sql 权限 0600，Git 和 Vercel 均排除；禁止打印到聊天或提交。不要重复执行 Cron 初始化脚本。
- 网站 DNS 保留 Squarespace Nameservers：A @ → 216.150.1.1 和 216.150.16.1；www CNAME → 217075c645c4eec7.vercel-dns-016.com。

## 已验证的功能

以下云端结果来自用户实际操作反馈，不冒充自动端到端测试：

- 教练注册、邮箱验证、登录后台成功。
- 测试学员注册、真实预约、教练和学员自动收件成功。
- 改期、取消、邮件开关均通过；关闭学员通知后，教练仍可收到通知。
- 改期界面修复后复测通过：直接列出时段，必须手动选择，打开时刷新空闲时间，无时段时有明确提示。
- 新改期邮件包含原时间、新时间、时区和原因，用户确认复测通过。数据库保存操作时的时间内容；旧邮件不补发。
- 用户确认 A 推荐码注册 B、邮箱验证后推荐数和教练后台记录通过。
- 用户确认 A 的专属训练计划可查看并收到通知，B 不可见。
- 用户确认 A 的私密档案对 A/B 都不可见，共享后只有 A 可见。
- 本地 38 项测试通过：SQL 权限/隔离、预约规则、时间/夏令时、推荐、邮件授权/重试/开关、历史通知内容等。类型检查和 Vercel 正式构建通过。
- 内置浏览器已验证本机演示的改期选项、不选择时阻止提交、选择后正确保存；正式站未登录，不掌握用户密码。

## 仍待完成或确认

- Vercel Git 连接已完成：CWT96/yvone-fitness，生产分支 main；已通过普通文档提交验证 Git 自动生产部署 READY。后续数据库迁移仍需单独运行。
- GitHub Actions：当前凭据缺 workflow 权限，模板在 scripts/github-actions-ci.yml，尚未启用；不是现有网站运行的前提。
- ACCEPTANCE.md 中其余远端验收未全部覆盖，例如密码恢复、真实教练代预约、同一时段并发预约、账号停用/恢复、课前提醒及手机布局。不要将本地测试宣称为云端这些项目已通过。
- Stripe 按用户要求只做购买页面，未接入收款。
- 之后将 GitHub 仓库改 Private（用户明确说晚点改，未授权现在更改）。

## 下一步

核心业务实际验收及 Git 自动部署验证通过；操作说明在 操作说明.md。其余尚未覆盖的远端验收范围已在上方列明，不能宣称全部完成。避免重复已通过的手工测试。所有私密 Key 直接填平台，不在聊天里发送。

## 2026-09-26 — 看板排版与购课须知

- 学员端隐藏计划归档规则；教练端保留。
- 重排学员看板内边距、资料头部、四项核心统计、辅助数据及内容区域；390px 手机和桌面浏览器验证无横向溢出。
- 新增时间并预约移到训练时间选项第一项，无时段时同样提供入口，保留已选学员。
- 购买课程页永久完整展示用户提供的三项购课须知与结语，顶部直达链接；会员预约、改期、取消和相关邮件加入针对性提醒。
- 当前单次/包月产品及有效期执行逻辑不变；须知中入门/标准/优选与线上 3/12 个月套餐尚未配置，三个月自动作废不是本次新增功能。
- 88 项测试、类型检查、生产构建通过；浏览器验证学员提示隐藏、须知全文、时间选项位置与移动排版。正式付款仍关闭。

## 2026-09-26 — 长期套餐与教练新时间改期

- 教练改期窗口第一项支持新增时间并改期，学员仅能选择已开放时段。允许与当前时段部分重叠；同事务撤下旧时段并改期，冲突回滚，原预约 ID 和事件历史保留。
- 增加 3、12 个月不限次套餐及每位学员的独立总价格；空价格不能购买。订单、Stripe 描述、日历有效期、重复通知与价格隔离均已接入。到期手动续购，保持测试模式。
- 202609260002_long_packages_reschedule.sql 已应用 Fitness 数据库。90 项测试、类型检查、构建通过；浏览器验证教练改期保存和学员四种套餐显示。

## 2026-09-26 — 邮件须知覆盖修正

- 不再仅限 member 收件人；教练和学员的预约、改期、取消、课前、缺席及付款通知均附对应规则。HTML 使用独立提示框和完整须知链接，纯文本同步提醒。
- 删除购买卡片“金额只对你和教练可见。”。91 项测试及构建通过，含两种收件角色、六类通知、HTML 与纯文本验证。
- 未切换正式收款；仅向用户提供正式 Stripe 配置步骤。

## 2026-09-26 — 品牌拼写

- 标签页标题、登录页、对话框和发信默认品牌统一 Yvonne Fitness。
- 用户报告正式 Stripe 配置已部署；未在本次更改支付配置。Vercel 项目改名由用户操作，应用通过项目 ID 关联；代码未使用 OIDC Federation。
